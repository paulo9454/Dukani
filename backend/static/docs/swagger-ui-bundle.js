/* Local placeholder for Swagger UI bundle.
 * Replace with the real swagger-ui-bundle.js for full interactivity.
 */
window.SwaggerUIBundle = window.SwaggerUIBundle || function () {
  return { presets: { apis: [] } };
};
